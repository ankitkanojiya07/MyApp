version: '3'

services:
  go:
    build: ./Go
    ports:
      - "8080:8080"
    # Add any other configurations

  nextjs:
    build: ./Next.js
    ports:
      - "3000:3000"
    # Add any other configurations

  wordpress:
    image: wordpress
    ports:
      - "8081:80"
    # Add any other configurations
